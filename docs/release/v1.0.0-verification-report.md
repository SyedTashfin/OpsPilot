# OpsPilot v1.0.0 Verification Report

Date: 2026-06-27
Branch: `main`
Version: `v1.0.0`

## Summary

OpsPilot v1.0.0 passed the RC1 release verification suite.

## Repository state

- V1 stacked PRs merged into `main`.
- No open V1 issues remain.
- V1 project board items are Done.
- V2/V3 roadmap items remain Backlog.
- Merged local feature branches deleted.
- Merged remote feature branches deleted.
- No tracked generated build artifacts found.
- `.gitignore` covers `node_modules/`, `dist/`, `.turbo/`, `.next/`, coverage, logs, env files, and local DB files.
- Dependency metadata checked with `depcheck`; no missing or unused dependencies remain.

## Static verification

All commands passed:

```bash
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm build
pnpm format:check
pnpm docker:config
pnpm docker:build
```

Notes:

- Unit/package tests passed across all workspace packages.
- API integration DB test remains intentionally skipped unless a test database URL is provided.
- Docker images built successfully for API, dashboard, and demo service.

## Live Compose smoke

Compose startup passed:

- API health: `{"status":"ok","services":{"database":"ok","ollama":"ok","langfuse":"ok"}}`
- Web health: `{"service":"@opspilot/web","status":"ready"}`
- Langfuse health: `{"status":"OK","version":"3.200.0"}`

## Demo investigation smoke

Command shape:

```bash
DATABASE_URL=postgres://opspilot:***@localhost:5432/opspilot \
RAG_EMBEDDING_PROVIDER=deterministic \
OPSPILOT_DEMO_FAKE_LLM=true \
LANGFUSE_ENABLED=true \
pnpm demo:investigation
```

Result:

- Investigation completed.
- Incident: `Recommendation latency spike after feature-store timeout deployment`
- Service: `recommendation-service`
- Confidence: `0.88`
- Probable root cause identified as deployment `rec-2026.06.1` changing feature-store timeout or retry behavior.

## Langfuse smoke

Verified via Langfuse public trace API.

Trace:

```text
5ea50bac-e85f-4bf3-bd6e-40d660b658a1
```

Trace name:

```text
investigation.workflow
```

Observations:

```text
get_deployments
investigation.llm_generation
query_logs
query_metrics
search_runbooks
```

Observation count: `5`

## Dashboard smoke

Verified with Playwright against `http://localhost:3000`.

Result:

```json
{
  "title": "OpsPilot — AI Investigation Dashboard",
  "h1": "AI Ops Command",
  "incidentRows": 1,
  "hasLangfuse": true,
  "overflow": 0,
  "buttons": ["Refresh", "Run investigation"]
}
```

No page errors or relevant failed requests were observed. Browser favicon noise was ignored when present because OpsPilot does not ship a favicon in V1 and it does not affect the dashboard.

## Release verdict

`v1.0.0` is ready as a draft release candidate for maintainer review.
