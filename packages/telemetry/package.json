{
  "name": "@opspilot/telemetry",
  "version": "1.0.0",
  "private": true,
  "description": "Langfuse and OpenTelemetry helpers.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "langfuse": "^3.38.20"
  }
}
