{
  "name": "@opspilot/rag",
  "version": "1.0.0",
  "private": true,
  "description": "Runbook retrieval and indexing package.",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "@types/pg": "^8.11.11"
  },
  "dependencies": {
    "@opspilot/database": "workspace:*",
    "pg": "^8.13.1"
  }
}
